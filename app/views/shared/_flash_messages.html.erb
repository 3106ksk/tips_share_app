<% flash.each do |message_type, message| %>
  <div class="flex justify-center <%= flash_background_color(message_type) %> animate-fade-out px-4 py-2 rounded-md text-black mb-4">
    <%= message %>
  </div>
<% end %>
